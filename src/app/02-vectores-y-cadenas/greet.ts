export function greet(nombre: string){

    

    return "Hola "+nombre;

    /*
    "test-code-coverage": "ng test --code-coverage --watch=false --browsers= ChromeHeadless",
    "test-headless": "ng test --watch=false --browsers= ChromeHeadless"*/
}